<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.bestsort.bbslite.mapper.QuestionCountExtMapper">
    <resultMap id="BaseResultMap" type="cn.bestsort.bbslite.pojo.model.QuestionCount">
        <id column="id" jdbcType="BIGINT" property="id" />
        <result column="question_id" jdbcType="BIGINT" property="questionId" />
        <result column="view" jdbcType="BIGINT" property="view" />
        <result column="follow" jdbcType="BIGINT" property="follow" />
        <result column="thumb_up" jdbcType="BIGINT" property="thumbUp" />
        <result column="gmt_create" jdbcType="BIGINT" property="gmtCreate" />
        <result column="gmt_modified" jdbcType="BIGINT" property="gmtModified" />
    </resultMap>


    <update id="incViewCount" parameterType="cn.bestsort.bbslite.pojo.model.QuestionCount">
        update question_count
        set
            view = view + #{view,jdbcType=BIGINT}
        where question_id = #{questionId}
    </update>

    <update id="updateCommentCount" parameterType="cn.bestsort.bbslite.pojo.model.QuestionCount">
        update question_count
        set
            comment = comment + #{comment,jdbcType=BIGINT}
        where question_id = #{questionId}
    </update>

    <update id="updateFollowCount" parameterType="cn.bestsort.bbslite.pojo.model.Question">
        update question_count
        set
            follow = follow + #{follow,jdbcType=BIGINT}
        where question_id = #{questionId}
    </update>
</mapper>